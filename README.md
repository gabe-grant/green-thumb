# Green Thumb Plant Repository

## Local Development
run `npm install`

Node v17+ requires react-scripts v5. Until updated, you must run `export NODE_OPTIONS=--openssl-legacy-provider`

and then `npm start`.